apiVersion: v1
kind: Service
metadata:
  name: ddiring-backend-asset-service
spec:
  selector:
    app: ddiring-backend-asset
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30008
  type: NodePort